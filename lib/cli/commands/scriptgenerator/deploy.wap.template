:: Deployment
:: ----------

echo Handling .NET Web Application deployment.

:: 1. Build to the temporary path
%MSBUILD_PATH% {MSBuildArguments}
IF %ERRORLEVEL% NEQ 0 goto error

:: 2. KuduSync [From] [To] [Manifest]
%KUDU_SYNC_COMMAND% -q -f "%DEPLOYMENT_TEMP%" -t "%DEPLOYMENT_TARGET%" -n "%NEXT_MANIFEST_PATH%" -p "%PREVIOUS_MANIFEST_PATH%"
IF %ERRORLEVEL% NEQ 0 goto error
